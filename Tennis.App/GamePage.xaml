<Page x:Class="Tennis.App.GamePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Tennis.App"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Loaded="Page_Loaded"
      Title="GamePage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <DataGrid x:Name="gameList" CanUserAddRows="False" CanUserDeleteRows="False" CanUserReorderColumns="False" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Game number" Binding="{Binding GameNumber}"/>
                <DataGridTextColumn Header="Member" Binding="{Binding Member.FullName}"/>
                <DataGridTextColumn Header="League" Binding="{Binding League.Name}"/>
            </DataGrid.Columns>
        </DataGrid>
        <Grid Name="GameCreate" Grid.Row="1"/>
    </Grid>
</Page>
